This folder contains council-related structured data.

- `council-members.json` stores the council member directory.
- Images are expected to live in `glasgow-va/images/council/` with filenames derived from the member names, e.g. `paul_williams.webp`.
